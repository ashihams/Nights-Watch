# Prefer the repo-root casting.yaml (judge-facing).
# Kept here for local Foundry workflows under infra/signoz/.
apiVersion: v1alpha1
kind: Installation
metadata:
  name: nights-watch-signoz
spec:
  deployment:
    mode: docker
    flavor: compose
  mcp:
    spec:
      enabled: true
